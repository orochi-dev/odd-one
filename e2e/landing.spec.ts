import { expect, test } from "@playwright/test";
test("landing page has the complete playable story",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/go low/i})).toBeVisible();await expect(page.getByText("Interactive preview")).toBeVisible();await page.getByRole("button",{name:/run the reveal/i}).click();await expect(page.getByText(/stood alone/i)).toBeVisible();await expect(page.getByRole("heading",{name:/simple rules/i})).toBeVisible();await expect(page.getByRole("link",{name:/enter the show/i}).first()).toHaveAttribute("href","/play");});
test("network chooser is functional",async({page})=>{await page.goto("/play");await expect(page.getByRole("heading",{name:/same game/i})).toBeVisible();await expect(page.getByRole("link",{name:/celo/i})).toHaveAttribute("href","/play/celo");await expect(page.getByRole("link",{name:/stacks/i})).toHaveAttribute("href","/play/stacks");});
test("unconfigured app does not invent live rooms",async({page})=>{await page.goto("/play/celo");await expect(page.getByText("Setup required")).toBeVisible();await expect(page.getByText(/not wired yet/i)).toBeVisible();await expect(page.getByText(/never substitutes preview records/i)).toBeVisible();});
