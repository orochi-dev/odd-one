import { fireEvent, render, screen, within } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import { LandingDemo } from "@/components/landing-demo";
describe("LandingDemo",()=>{it("runs a clearly labeled simulated reveal",()=>{render(<LandingDemo/>);expect(screen.getByText("Interactive preview")).toBeInTheDocument();expect(screen.getByLabelText("Preview player 1 is still hidden")).toBeInTheDocument();const picker=screen.getByRole("group",{name:/choose your preview number/i});const option=within(picker).getByRole("button",{name:/pick 5 for the preview/i});expect(option).toHaveAttribute("aria-pressed","false");fireEvent.click(option);fireEvent.click(screen.getByRole("button",{name:/run the reveal/i}));expect(screen.getByLabelText("Preview player 1 revealed 5")).toBeInTheDocument();expect(screen.getByText("Your 5 was crowded out. Number 4 stood alone.")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:/play it again/i}));expect(screen.getByRole("button",{name:/run the reveal/i})).toBeInTheDocument();});});
